Generate complete executable R code to:
1) read csv_path from mapped_plan.csv_schema.csv_path
2) use mapped_plan.trt_group_name as group column
3) preserve DOCX row/column order exactly from tables[].ordered_rows and tables[].columns
4) fill each data row using mapping_tasks candidate_csv_column + row format_hint
5) print each final table in order

Rules:
- Output plain executable R script only in code field.
- No markdown fences.
- Only load R packages that are actually used in the script. Do not include unused libraries such as `scales`.
- Only use packages from this allowlist: `data.table`, `stringr`, `stats`. Prefer base R when possible, and do not use any other package or namespaced function.
- If a mapping is missing, emit TODO comment for that row.
- Use format_hint to decide display pattern.
- For categorical rows, prefer `mapping_tasks[].category_value_map[row_label]` as the matching value; fallback to normalized label matching only if missing.
- DO NOT hardcode mapped_plan content into R (no `tables <- list(...)`, `ordered_rows <- list(...)`, `mapping_tasks <- list(...)`).
- DO NOT embed full mapped_plan JSON string in R (forbidden: `json_payload <- '{...}'` + `fromJSON(json_payload)`).
- You may materialize minimal table/mapping constants needed for execution, but avoid dumping the full mapped_plan JSON blob.
- MUST implement explicit DOCX-column to TRT-value mapping:
  - compute `trt_levels <- unique(data[[trt_group]])`
  - normalize DOCX labels and TRT values (tolower + trimws + hyphen normalization) and match by label only
  - do not use positional fallback (`docx_columns[i] -> trt_levels[i]` is forbidden)
  - if unmatched, keep mapping as `NA` and leave output cells blank for that treatment column
  - then filter using mapped TRT value, NOT raw DOCX column label
- When printing final tables, preserve the row-label column as visually blank-header rather than defaulting to names like `V1`.
- Preserve row hierarchy in the printed table: group header rows should be flush left, while child statistic/category rows should be indented for readability.
- Build treatment display headers using DOCX column labels plus actual group sizes when available, e.g. `Treatment A (N=121)`.
- Print final tables with sufficient console width so all treatment columns stay on the same printed table rather than wrapping awkwardly.
- Prefer deterministic plain-text table output over raw `print(data.frame(...))` so extra console indentation and default dataframe formatting do not distort the shell layout.
- When computing print widths, use display width rather than plain character count so labels with Unicode characters like `Min–max` and `≥65` stay aligned.
- Include category normalization before matching labels (e.g., tolower + trimws + hyphen normalization).
- Before printing each final table, replace display `NA` values in the output matrix/data frame with empty strings so group header rows render blank instead of `<NA>`.

Output schema:
{ "code": "<R script>", "warnings": ["..."] }

Input:
{payload_json}
